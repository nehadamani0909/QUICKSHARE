datasource db {
  provider = "sqlite"
}

generator client {
  provider = "prisma-client-js"
}

model Share {
  id        String   @id @default(uuid())
  content   String?
  fileUrl   String?
  expiresAt DateTime
  createdAt DateTime @default(now())
}